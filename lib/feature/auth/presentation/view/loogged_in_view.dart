import 'package:flutter/material.dart';

// TODO(sametdmr): temporary view handle it

class DummyLoggedInView extends StatelessWidget {
  const DummyLoggedInView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('LOGIN SUCCESS')),
    );
  }
}
